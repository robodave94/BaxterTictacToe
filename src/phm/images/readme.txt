for template of "x", template_x03.png was captured at height of "table z + 0.1", works from "tz+0.15 ~ tz+0.06", linear transform, roation works too.

for template of "o", template_o_outside_contour3.png captured at height of of "table z + 0.1", 
